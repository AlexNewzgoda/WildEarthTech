#pragma kernel Copy

float4 Offset;
Texture2D<float4> Source;
RWTexture2D<float4> Target;

[numthreads(8,8,1)]
void Copy (uint3 id : SV_DispatchThreadID)
{
    float2 pos = id.xy + Offset.xy;
    Target[pos] = Source[pos];
}
